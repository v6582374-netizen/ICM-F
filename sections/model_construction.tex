
\section{模型构建与实现}

\subsection{职业选择作为建模决策}

问题的前提要求从STEM、技工和艺术领域各选择一个职业。
我们没有将这一步骤视为临时假设（主观选择），而是明确地将职业选择建模为一个优化问题。
这确保了后续关于AI驱动影响和教育建议的分析建立在可重现且数据驱动的框架之上。
职业选择的核心目标不是所有职业的代表性，而是\emph{解释性对比}。
具体而言，三个职业的任务结构应当在与GenAI的交互方式上存在显著差异。
除了机制对比外，问题还明确强调数据驱动的推理以及后文的政策建议。因此，所选职业同时需要有完备的数据支撑以及政策相关性。

\subsubsection{基于O*NET的职业特征参数客观化}

为避免职业特征参数 $\textit{digit}(x)$、$\textit{physical}(x)$ 和 $\textit{iprisk}(x)$ 的主观赋值，我们采用美国劳工部维护的\textbf{O*NET}职业数据库作为统一数据源。
O*NET基于专家调查和从业者反馈，为每个职业提供大量\emph{数值量表描述符}，大多数指标自然地给出在$0$--$100$之间的实数上，适合直接用于定量建模。

我们不试图为每个维度找到"唯一正确"的指标，对于每个特征轴，我们选择少量语义上直接对应且可下载的O*NET描述符，并将其用作该轴的代理变量。
对于没有描述（descriptor字段缺失）的职业直接剔除。
基于问题陈述和数据可用性约束，我们识别出三个主导维度，这些维度控制着AI影响在各职业间的异质性：
\begin{itemize}
    \item \textbf{数字化程度$digit(x)$}：核心任务可以以数字方式表示、自动化或增强的程度；
    \item \textbf{物理锚定$physical(x)$}：任务执行对现场物理存在或与环境的手动交互的依赖程度；
    \item \textbf{知识产权和署名风险$iprisk(x)$}：价值创造对作者身份、原创性和所有权的敏感程度。
\end{itemize}

表~\ref{tab:onet_descriptor_mapping} 列出了每个特征轴所对应的O*NET描述符及其可辩护性说明。
从O*NET下载所选descriptor的数值（均为0–100量表），再在全职业集合上做min-max归一化到[0,1]，得到digit(x), physical(x), iprisk(x)。

\begin{table}[htbp]
\centering
\caption{三维特征轴与O*NET描述符的对应关系}
\label{tab:onet_descriptor_mapping}
\begin{tabular}{@{}p{2.2cm} p{4.0cm} p{4.8cm} p{3.0cm}@{}}
\toprule
\textbf{特征轴} & \textbf{客观取值方式} & \textbf{O*NET描述符} & \textbf{可辩护性} \\
\midrule
$digit(x)$\newline 数字化程度 & 工作行为中对计算机/电子媒介的依赖 & Working with Computers~\cite{onetWorkingWithComputers}；\newline E-Mail~\cite{onetEmail} & 直接刻画工作是否以计算机与电子信息流为主 \\
\addlinespace
$physical(x)$\newline 物理锚定程度 & 手部操作+身体姿态负担等物理现场需求 & Using Hands to Handle Objects~\cite{onetUsingHands}；\newline Kneeling/Crouching~\cite{onetKneeling} & 直接量化现场动手强度与工位姿态限制 \\
\addlinespace
$iprisk(x)$\newline 产权/归因风险 & 产出是否以原创表达/创意贡献为核心 & Originality~\cite{onetOriginality}；\newline Thinking Creatively~\cite{onetThinkingCreatively} & 用原创表达强度作为可复现的风险代理变量 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{归一化与可重现性}
对于每个职业 $x$，令 $v_m(x)$ 表示其在O*NET描述符 $m$ 下的原始评分（$0$--$100$）。
我们在整个职业集上应用最小-最大归一化：
\begin{equation}
    z_m(x)=\frac{v_m(x)-\min_{y}v_m(y)}{\max_{y}v_m(y)-\min_{y}v_m(y)} \in [0,1],
\end{equation}
其中 $y$ 遍历所有职业。设特征轴 $k \in \{digit, physical, iprisk\}$ 对应的描述符集合为 $M_k$（见表~\ref{tab:onet_descriptor_mapping}，每个特征轴对应2个O*NET描述符），则对同一特征轴下的多个描述符取等权平均：
\begin{equation}
    \textit{feature}_k(x) = \frac{1}{|M_k|} \sum_{m \in M_k} z_m(x).
\end{equation}
由此得到 $\textit{digit}(x)$、$\textit{physical}(x)$ 和 $\textit{iprisk}(x)$。
该过程完全基于公开可用数据和明确规则，确保参数值的可重现性和可审计性。


每个候选职业$x$由一个归一化的三维特征向量表示，可以作为3Dscatter中的一个数据点。
\begin{equation}
    f(x)=\big(digit(x),\,physical(x),\,iprisk(x)\big)\in[0,1]^3,
\end{equation}
其中较高的值表示相应特征的存在更强。

\[
Area3D(x_S, x_T, x_A) = \frac{1}{2} \left\lVert \big(f(x_T) - f(x_S)\big) \times \big(f(x_A) - f(x_S)\big) \right\rVert_2,
\]
其中 $x_S \in \text{STEM}$ ，$x_T \in \text{Trade}$ ，$x_A \in \text{Arts}$ 。\\
为衡量三种职业在特征空间中的异质性，我们用三点在三维空间中所形成三角形的面积作为衡量标准，定义为

我们以最大化该三角形面积为目标，形式化为
\[
\max_{x_S \in S,\; x_T \in T,\; x_A \in A} \; Area3D(x_S, x_T, x_A).
\]
此外，为便于后续可视化表达，定义其在二维投影下的面积为
\[
Area2D_{\pi}(x_S, x_T, x_A) = \frac{1}{2} \left| \det \left([\pi(f(x_T)) - \pi(f(x_S)),\ \pi(f(x_A)) - \pi(f(x_S))] \right) \right|,
\]
其中 $\pi(\cdot)$ 表示三维特征到二维空间的投影映射。

我们可视化了三维异质性及其二维投影，以验证所选职业在特征空间中占据不同区域（图~\ref{fig:heterogeneity3d} 和图~\ref{fig:heterogeneity2d}）。

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{figures/fig_heterogeneity_3d_all_points_strict.pdf}
\caption{基于O*NET描述符（全局归一化）的全体职业三维异质性点云与最优三角面（strict映射）。}
\label{fig:heterogeneity3d}
\end{figure}

我们先在全点云上求解无约束的最大面积三角形，随后根据赛题要求加入“STEM/技工/艺术三域各取一职”的约束，在可行域内重新最优化，得到约束后的最优三点：STEM（Robotics Engineers）、技工/Trades（First-Line Supervisors of Mechanics, Installers, and Repairers）与艺术/媒体（Court Reporters and Simultaneous Captioners）。其三角形面积为 $0.262221$，最短边长为 $0.751378$，该约束最优结果刻画三域职业在三维特征空间上的最大异质性跨度。

\vspace{0.6\baselineskip}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{figures/fig_heterogeneity_2d_projections_all_points_strict.pdf}
\caption{全体职业异质性空间的二维投影与最优三点标注（strict映射，全局归一化）。}
\label{fig:heterogeneity2d}
\end{figure}

\begin{table}[H]
\centering
\caption{最大三角形面积解中三域（STEM、技工、艺术）所选代表职业与 O*NET-SOC 代码。三个点分别代表在三维特征空间选出的异质性最大化顶点，即模型异质性指标的最优解。}
\label{tab:heterogeneity_tri_vertices}
\begin{tabular}{c c p{6.6cm} p{4.2cm}}
\toprule
\textbf{领域} & \textbf{O*NET-SOC代码} & \textbf{职业名称} & \textbf{3D最大面积解中的角色} \\
\midrule
STEM  & 17-2199.08 & \parbox[t]{6.6cm}{Robotics Engineers} & \parbox[t]{4.2cm}{最优三角形顶点（S）} \\
Trade & 49-1011.00 & \parbox[t]{6.6cm}{First-Line Supervisors of Mechanics, Installers, and Repairers} & \parbox[t]{4.2cm}{最优三角形顶点（T）} \\
Arts  & 27-3092.00 & \parbox[t]{6.6cm}{Court Reporters and Simultaneous Captioners} & \parbox[t]{4.2cm}{最优三角形顶点（A）} \\
\bottomrule
\end{tabular}
\end{table}
三种职业均包含于官方对三个领域的定义\cite{onet2019structure}。

\paragraph{稳健性证据与数值结果}
为验证最大异质性三角形选取结果的稳健性，我们在不同参数设置与采样条件下均重复求解，并发现核心指标高度一致。具体地，基于严格映射与全局归一化方法，三维最大面积三角形的面积为
\begin{equation*}
    \mathrm{Area3D} = 0.262221
\end{equation*}
最短边长为
\begin{equation*}
    \mathrm{min\_edge\_length} = 0.751378
\end{equation*}
进一步，在不同采样规模 $M\in\{200, 400, 800\}$ 下，最优三角形面积的相对差异均为 $0.0\%$，表明所建模型和结论具备高度的鲁棒性与可重复性。



\subsection{职业演化的模型选择}

问题的第二个核心要求是构建一个\emph{数据驱动}的未来演化模型，并明确说明“当前轨迹（current trajectory）”以及会因GenAI而发生变化的驱动因素。
在我们框架中，预测对象不直接设为“职业岗位数”，而是设为\textbf{职业内部的任务需求结构}：即在给定GenAI采纳轨迹下，各任务在职业内的重要性（或相对需求）如何随时间变化。
因此，我们首先构建 Task DNA 的任务权重 $w_{ij}$，并以权威来源重标注任务级替代/增强强度 $(s_{ij},c_{ij})$，进而得到任务级净冲击
\begin{equation}
    \mu_{ij}^{task}=c_{ij}-s_{ij},
\end{equation}
并将“当前轨迹”形式化为外生采纳路径 $A(t)\in[0,1]$（快速/中速/慢速情景），用于驱动任务需求随时间的演化。

\vspace{0.5\baselineskip}

\subsubsection{未来演化的可计算定义}
为消除题目中“future”的时间模糊性，我们将未来定义为\textbf{任务层面的可计算事件}：当某个关键任务在职业内的相对需求（份额）下降至一个足够小的阈值 $\epsilon$ 以下时，即认为该任务在可操作意义上“趋近于消失”。
具体地，若 $p_{ij}(t)$ 表示职业 $i$ 中任务 $j$ 在时刻 $t$ 的相对需求份额（定义见后文），则定义任务 $j$ 的“有效归零时间”为
\begin{equation}
    \tau_{ij}(\epsilon)=\min\{t:\ p_{ij}(t)\le \epsilon\}.
\end{equation}
该定义可直接用于回答“未来何时发生显著变化”的问题，并允许通过不同 $\epsilon$ 取值进行敏感性分析。

\subsubsection{模型框架选择与理由}
我们采用一个以任务为核心的最小化框架：
(1) 职业由任务集合组成，任务具有可从O*NET复现的权重；
(2) GenAI的影响在任务级体现为替代/增强（互补）强度；
(3) “当前轨迹”由采纳路径 $A(t)$ 表示，采纳越深，任务级冲击释放越充分。
该框架直接对应任务分解视角下的技术冲击机制（技术以任务为单位改变工作内容），并避免对“职业岗位数”做强外推。

\subsection{任务层动态预测模型（Task-first）}

在上述论证之后，我们给出任务层预测的数学公式。
模型包含两类输入：\textbf{任务结构}（来自O*NET任务数据）与\textbf{发展轨迹}（GenAI采纳路径 $A(t)$）。
预测输出为：每个职业内部任务的相对需求演化 $p_{ij}(t)$，以及“有效归零时间” $\tau_{ij}(\epsilon)$。

\subsubsection{任务权重（Task DNA）与任务净冲击}

\paragraph{任务DNA的定义与数据来源}
对于职业 $i$，我们选取其\textbf{前20项核心任务}作为其任务DNA（任务索引 $j = 1, \ldots, 20$）。每项任务 $j$ 取自O*NET任务描述 $Task_j$，并从同一数据库中检索两个基于量表的描述符：
\begin{itemize}
    \item \textbf{任务重要性} $IM_j$（范围 0--100）；
    \item \textbf{任务频率} $FR_j$（范围 0--100，或同等的频率量表）。
\end{itemize}
由于这些值可以直接从O*NET数据库下载，因此任务列表和基础度量是完全\textbf{可复现}的。

\paragraph{任务权重的推导}
我们将任务权重定义为重要性 $IM_j$ 与频率 $FR_j$ 乘积的归一化值。令未归一化的权重为：
\begin{equation}
    \tilde{w}_j = IM_j \cdot FR_j,
\end{equation}
然后通过下式进行归一化：
\begin{equation}
    w_{ij} = w_j = \frac{\tilde{w}_j}{\sum_{k=1}^{20} \tilde{w}_k}, \quad \sum_{j=1}^{20} w_j = 1.
\end{equation}
如果所选SOC（标准职业分类）代码缺少频率量表 $FR_j$，则应用降级规则，仅使用重要性值（即设 $\tilde{w}_j = IM_j$），并随后通过敏感性分析测试其稳健性。

\paragraph{任务DNA与权威s,c分布（三职业）}
为确保任务级建模输入可追溯，我们为三职业分别展示两类可视化图表：

\begin{itemize}
    \item \textbf{IM--FR 散点图}（左图）：横轴为任务重要性 $IM_j$，纵轴为任务频率 $FR_j$，每个点代表一项任务，点的面积与任务权重 $w_j$ 成正比。该图的作用是\emph{直观呈现职业内部的任务结构}：点越大、位置越靠右上角，表明该任务在职业中占据越核心的地位。通过观察点的分布形态，可以快速判断职业的任务集中度（少数任务主导 vs. 多任务均衡）以及任务的重要性--频率相关性。
    \item \textbf{权威 $s,c$ 档位分布图}（右图）：展示经权威评估重标注后，各任务在替代强度 $s$ 和增强强度 $c$ 离散档位上的分布。该图的作用是\emph{揭示GenAI对该职业的潜在冲击模式}：若多数任务落在高 $s$ 低 $c$ 区域，表明该职业面临较高的自动化替代风险；反之，若多数任务落在低 $s$ 高 $c$ 区域，则GenAI更可能增强而非替代人类工作。
\end{itemize}

相应图示见图~\ref{fig:task_dna_17_pair}；其余两职业图示移至附录~\ref{fig:task_dna_49_pair}--\ref{fig:task_dna_27_pair}。

\noindent\textbf{职业 17-2199.08：机器人工程师。}
\begin{figure}[ht]
\centering
\begin{minipage}{0.62\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/task_dna_17-2199.08_weights.pdf}
\end{minipage}\hfill
\begin{minipage}{0.35\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/task_dna_17-2199.08_authoritative_sc_distribution.pdf}
\end{minipage}
\caption{职业 17-2199.08 的任务DNA与权威 $s,c$ 分布。（a）任务DNA权重分布：IM--FR 散点图，点面积与 $w$ 成正比；（b）权威 $s,c$ 档位分布。}
\label{fig:task_dna_17_pair}
\end{figure}
\noindent\textbf{图示解读（17-2199.08）}：IM--FR 散点图中任务点集中在中高重要性与中等频率区间，且点面积差异不大，表明该职业的核心任务权重分布较为均衡，未出现单一任务强烈主导的结构。权威 $s,c$ 档位分布显示 $s$ 以 0.50 为主、辅以少量 0.30 与极少 0.90，而 $c$ 明显向高档位偏移（0.90 占多数）。这表明该职业的主要任务更可能体现为\emph{增强型冲击}，即GenAI倾向于提升系统集成与控制类任务的效率，而非直接替代。

\noindent\textbf{职业 49-1011.00：一线主管（机械/安装/维修）。}（图见附录~\ref{fig:task_dna_49_pair}）
\noindent\textbf{图示解读（49-1011.00）}：IM--FR 散点图显示任务分布覆盖较宽频率区间，但整体集中在中高重要性区域，点面积更均匀，说明管理与监督类任务构成了稳定的核心组合，而非由少数高权重任务驱动。权威 $s$ 档位呈现更分散的结构（0.10/0.30/0.50/0.70 同时存在），而 $c$ 以 0.50 与 0.70 为主，少量任务达到 0.90。该模式反映出该职业的任务冲击方向不单一：既包含可被流程化替代的监督环节，也包含可被工具强化的协调与资源管理环节。

\noindent\textbf{职业 27-3092.00：法庭速录与字幕。}（图见附录~\ref{fig:task_dna_27_pair}）
\noindent\textbf{图示解读（27-3092.00）}：IM--FR 散点图中任务点整体位于高重要性与高频率区域，且最大权重点明显大于其余点，说明该职业对少数高频核心任务（如记录、转写类任务）的依赖更强，任务结构集中度更高。权威 $s,c$ 档位分布显示 $s$ 在 0.90 与 0.30 间呈两端分化，而 $c$ 主要集中在 0.30 与 0.50。该结构意味着该职业中高频核心任务更接近\emph{替代型冲击}（高 $s$、低 $c$），与速记/转写类任务的自动化趋势一致。

\paragraph{任务影响强度的离散化量表与证据约束}
为了避免主观的连续评分，对于替代强度 $s_{ij}$ 和增强强度 $c_{ij}$，我们采用一个离散的等级量表：
\begin{equation}
    s_{ij},\ c_{ij} \in \{0.10,\ 0.30,\ 0.50,\ 0.70,\ 0.90\}.
\end{equation}
其中：
\begin{itemize}
    \item $s_{ij}$ 衡量任务 $j$ 被生成式人工智能（GenAI）驱动自动化/压缩的可行性。
    \item $c_{ij}$ 衡量通过工具增强所获得的效率、质量或工作流程上的提升。
\end{itemize}

\subsection{基于权威基准的GenAI能力量表与可审计映射}

为将任务层 $(s,c)$ 的来源从“主观估计”改为“可复现规则”，我们将GenAI能力表述为一套\textbf{权威基准$\rightarrow$能力分段$\rightarrow(s,c)$映射}的离散量表。该量表仅依赖公开的基准分数与可审计的分段规则，从而支持逐层追溯。

\subsubsection{机制审计与最短修复路径}
为避免解释力削弱，我们先对现有表格进行机制审计，并给出最短修复路径（表~\ref{tab:genai_audit}）。
\begin{table}[htbp]
\centering
\caption{GenAI能力表的机制审计与修复路径}
\label{tab:genai_audit}
\small
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{p{2.5cm} p{3.8cm} p{4.0cm} p{3.9cm}}
\toprule
\textbf{问题点} & \textbf{具体表现} & \textbf{可解释性风险} & \textbf{最短修复路径} \\
\midrule
$s_k,c_k$仍是“任务分析估计” &
例如 D3 给出 $s=0.9,c=0.7$；D1 给出 $s=0.5,c=0.85$ &
缺乏“为何不是相邻值”的可审计依据 &
用统一分段规则：先由权威基准得到 $M_k$，再由方向系数 $\rho_k$ 约束 $(s_k,c_k)$ \\
\addlinespace
证据混入不稳定/非权威来源 &
例如博客或二手表述的成功率 &
证据链薄弱，易被质疑“挑数据” &
仅保留论文/官方技术报告/官方基准页；其他仅作补充说明 \\
\addlinespace
指标口径不统一 &
accuracy、WER、success rate 并存 &
横向比较缺乏共同尺度 &
用能力分段把异质指标统一到 $M_k$ 的离散档位 \\
\addlinespace
D9 的高分来自特定管线 &
并非通用VLM的标准成绩 &
可能被质疑偷换概念 &
明确标注为“上界/可达性证据”，并允许敏感性下调 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{能力量表：$M_k\rightarrow\rho_k\rightarrow(s_k,c_k)$}
\paragraph{能力分段（$M_k$）}
我们不做连续拟合，而采用阈值区间确定能力等级，并在区间内取代表值 $M_k$（见表~\ref{tab:genai_scale}）：
\begin{itemize}
    \item 高（$\ge 0.85$）、较高（$0.70$--$0.85$）、中等（$0.50$--$0.70$）、低（$0.30$--$0.50$）、极低（$<0.30$）。
\end{itemize}
该分段仅依赖公开分数与阈值，避免“伪精确”。

\paragraph{方向系数（$\rho_k$）}
我们用连续方向系数刻画“替代/互补/平衡”的偏向强度：
\begin{itemize}
    \item $\rho_k<0$：偏替代（AI可独立完成可交付物）。
    \item $\rho_k>0$：偏互补（AI提供建议/辅助，人必须在环）。
    \item $\rho_k=0$：平衡（两边都强，净效应不确定）。
\end{itemize}

\paragraph{可审计赋值}
表~\ref{tab:genai_scale} 直接给出每个维度的 $M_k$、$\rho_k$ 及对应的 $(s_k,c_k)$ 取值，用于后续任务层映射与追溯。

\subsubsection{九维能力量表（含原始评分与证据）}
表~\ref{tab:genai_scale} 给出可直接落地的基线版本：每个维度包含原始评分、分段得到的 $M_k$、方向系数 $\rho_k$，以及对应的 $(s_k,c_k)$。
\begin{table}[htbp]
\centering
\caption{GenAI 九维能力映射总表：指标分段$\rightarrow M_k,\rho_k\rightarrow(s_k,c_k)$}
\label{tab:genai_scale}
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{2.6cm} p{3.6cm} p{4.6cm} p{2.2cm} p{1.0cm} p{1.0cm} p{0.9cm} p{0.9cm}}
\toprule
\textbf{维度（编号）} & \textbf{定义} & \textbf{代表性指标（来源 \& 分数）} & \textbf{能力等级（阈值规则）} & \textbf{$M_k$} & \textbf{$\rho_k$} & \textbf{$s_k$} & \textbf{$c_k$} \\
\midrule
D1 工程技术问题解决 &
运用工程知识分析解决技术问题（调试维护、技术支持） &
MMLU 86.4\%；ARC 96.3\%（GPT-4 报告\citep{openai2023gpt4}） &
高（$\ge 0.85$） & 0.90 & $+0.35$ & 0.50 & 0.85 \\
\addlinespace
D2 数据分析与评估 &
分析传感器/数值数据并据标准评估（质检、数据监控） &
GSM8K 92.0\%；DROP 80.9（GPT-4 报告\citep{openai2023gpt4}） &
较高（$0.70$--$0.85$） & 0.80 & $+0.20$ & 0.30 & 0.50 \\
\addlinespace
D3 算法编程与自动化 &
编写和修正代码脚本实现需求（软件算法开发、自动化流程） &
HumanEval 67.0\%；SWE-bench 修复 20\%\citep{openaicodex2021,swebench2024} &
中等（$0.50$--$0.70$） & 0.67 & $-0.20$ & 0.90 & 0.70 \\
\addlinespace
D4 现场感知与物理操作 &
感知物理环境及动手操作设备（现场检查、安全监督、设备调校） &
AutoGPT 实体环境任务 24\%；视觉识别 $\approx 85\%+$\citep{autogpt2023,imagenet2009} &
低（$0.30$--$0.50$） & 0.25 & $+0.40$ & 0.10 & 0.50 \\
\addlinespace
D5 规划与流程调度 &
制定任务计划、优化流程顺序和资源分配（调度排程、进度管理） &
AutoGPT 自主任务完成率 $\sim 65\%$；工具使用成功率 $\sim 80\%$\citep{autogpt2023} &
中等（$0.50$--$0.70$） & 0.65 & $-0.10$ & 0.60 & 0.50 \\
\addlinespace
D6 协作沟通与培训 &
沟通协作、传授知识并协调团队（培训、跨部门协作） &
人类偏好：GPT-4 胜率 70.2\%\citep{openai2023gpt4} &
较高（$0.70$--$0.85$） & 0.70 & $+0.45$ & 0.30 & 0.75 \\
\addlinespace
D7 监督管理与质量控制 &
监督工作表现并确保质量达标（绩效管理、质检） &
HellaSwag 95.3\%\citep{openai2023gpt4} &
较高（$0.70$--$0.85$） & 0.80 & 0.00 & 0.68 & 0.68 \\
\addlinespace
D8 语言理解与文本生成 &
理解口语/文本并生成书面内容（转录、写作、翻译） &
Whisper WER $\approx 0.011$；GPT-4 文本偏好胜率 70\%\citep{whisper2022,openai2023gpt4} &
高（$\ge 0.85$） & 0.95 & $-0.40$ & 0.90 & 0.50 \\
\addlinespace
D9 信息管理与检索 &
有序存储组织信息并按需高速检索核验（档案管理、查询答复） &
DocVQA 99.16\%；Text-to-SQL $>80\%$\citep{docvqa2017,spider2018} &
高（$\ge 0.85$） & 0.90 & 0.00 & 0.30 & 0.30 \\
\bottomrule
\end{tabular}}
\end{table}
\begin{spacing}{0.9}
\noindent
\textbf{注：} 上表中能力等级按照统一阈值划分：\textbf{High} $\geq 0.85$，\textbf{Medium-High} $0.70$--$0.85$，\textbf{Medium} $0.50$--$0.70$，\textbf{Low} $0.30$--$0.50$，\textbf{Minimal} $<0.30$。\ $M_k$ 选取当前顶尖模型在相关指标上的代表性得分（0$\sim$1标准化后）。$\rho_k = c_k - s_k$ 表示GenAI对该维度人力需求的影响方向：正值为互补（提升人效使需求上升），负值为替代（可自动化使需求下降）。$s_k$ 和 $c_k$ 分别为我们根据任务分析估计的可替代率和可协同率。

总体来看，GenAI 在语言和编码等维度已达到高能力并呈替代效应，而在涉及物理、管理等维度时，仍主要作为辅助工具，与人类技能形成互补。
\end{spacing}

\paragraph{必要声明}
\begin{itemize}
    \item D9 的 DocVQA 99.16\% 可视为文档问答场景的上界证据，不等价于所有检索情境与数据库规模下的通用能力。
    \item D4/D5 采用 AutoGPT 端到端完成率与工具使用成功率作为下界证据，用于避免夸大“现场操作/流程调度”的可自动化程度。
\end{itemize}




为体现匹配强度对评分的影响，我们先根据维度默认值计算原始分值 $(s_{raw}, c_{raw})$，再用任务与维度的相似度强度 $r_{ij}$ 拉伸其偏离中值的幅度：
\begin{equation}
    s'_{ij}=0.5+(s_{raw}-0.5)\big(0.6+0.8\,r_{ij}\big),\quad
    c'_{ij}=0.5+(c_{raw}-0.5)\big(0.6+0.8\,r_{ij}\big).
\end{equation}
最后将 $s'_{ij}, c'_{ij}$ 投影到上述离散档位。对于每项任务 $j$，我们需要一个\textbf{1--2行的证据说明}，该说明由\textbf{可复现的语义匹配规则}生成，并辅以有限的人工抽查验证。此约束将评分从临时性判断转化为\textbf{有规则触发、且带有可审计标注的赋值过程}，并支持后续的不确定性处理（例如，权重扰动和基于区间的敏感性检查）。

\paragraph{任务层净冲击}
我们将任务层净冲击定义为：
\begin{equation}
    \mu^{task}_{ij}=c_{ij}-s_{ij}.
\end{equation}
$\mu^{task}_{ij}>0$ 表示GenAI更可能增强该任务（提高产出/质量/速度），$\mu^{task}_{ij}<0$ 表示该任务更可能被替代或显著压缩。

\subsubsection{将“发展轨迹”与“任务需求”紧密耦合：采纳混合机制}

题目要求“given the current trajectory and expected impacts of Gen-AI”，其中“current trajectory”可操作地对应为\textbf{采纳程度随时间的扩散}。
我们用外生的采纳路径 $A(t)\in[0,1]$ 表示在时间 $t$ 已采纳GenAI的工作流占比（或强度）。
直观上：在 $A(t)=0$ 时，任务结构保持基线；在 $A(t)$ 增大时，任务层净冲击 $\mu^{task}_{ij}$ 逐步释放，任务需求结构随之重排。

为避免凭空设定“绝对任务需求”的总量，我们以任务\textbf{相对需求权重}作为预测对象，并采用“采纳者/未采纳者混合”的最小机制：
未采纳部分保持基线权重 $w_{ij}$；采纳部分对任务权重施加与 $\mu^{task}_{ij}$ 单调一致的乘性变换（保证权重为正）。
定义未归一化任务权重
\begin{equation}
    q_{ij}(t)=w_{ij}\Big[(1-A(t))+A(t)\exp\big(\eta\,\mu^{task}_{ij}\big)\Big],
\end{equation}
其中 $\eta>0$ 为冲击强度缩放（无量纲），用于刻画“单位采纳带来的任务结构重排幅度”，并在敏感性分析中报告其影响。

将 $q_{ij}(t)$ 归一化得到任务相对需求份额
\begin{equation}
    p_{ij}(t)=\frac{q_{ij}(t)}{\sum_{k=1}^{J_i} q_{ik}(t)}.
\end{equation}
该定义保证 $\sum_j p_{ij}(t)=1$，从而直接刻画职业内部任务结构如何随采纳轨迹而演化。

\paragraph{解释性}
当 $A(t)$ 上升（GenAI扩散加深）时，若 $\mu^{task}_{ij}<0$，则 $q_{ij}(t)$ 相对缩小，$p_{ij}(t)$ 下降；反之若 $\mu^{task}_{ij}>0$，则该任务份额上升。
因此，$A(t)$ 将“发展轨迹”以严格数学方式映射为任务层需求结构的时间变化，从而满足题面关于“当前轨迹+预期影响”的建模要求。

\paragraph{未来时间点}
结合前述定义，任务的有效归零时间仍为
\begin{equation}
    \tau_{ij}(\epsilon)=\min\{t:\ p_{ij}(t)\le \epsilon\}.
\end{equation}
通过对Top任务的 $\tau_{ij}(\epsilon)$ 进行比较，我们可以直接回答：在不同采纳情景下，哪些任务会最先“消失”，哪些任务会长期保留，从而给出对该职业“未来”形态变化的可解释预测。



\subsection{将任务DNA聚合为职业级冲击参数：软件开发人员}

本节将软件开发人员任务DNA从任务级输入 $(w_{ij}, s_{ij}, c_{ij})$ 聚合为职业级冲击参数 $(\alpha_i,\beta_i,\mu_i)$，
这将输入演化方程 $\frac{dE_i(t)}{dt}=E_i(t)\big(g_i + A(t)(\beta_i-\alpha_i)\big)$。

\subsubsection{Top-$N$ 可用性与聚合}

在实践中，O*NET任务评分可能对某些任务缺失。
因此我们采用"Top-$N_i$ 可用任务"策略：对于职业 $i$，我们只保留具有足够评分以构建权重的任务，并重新归一化使得 $\sum_{j=1}^{N_i} w_{ij}=1$。
职业级替代压力、增强收益和净方向定义为
\begin{equation}
    \alpha_i=\sum_{j=1}^{N_i} w_{ij} s_{ij},\qquad
    \beta_i=\sum_{j=1}^{N_i} w_{ij} c_{ij},\qquad
\end{equation}

\subsubsection{可解释性分解：Top-$K$ 贡献下界}

为增强可解释性，我们分解高权重任务的贡献。定义部分和
\begin{equation}
    \alpha_i^{(K)}=\sum_{j\in \text{Top-}K} w_{ij}s_{ij},\qquad
    \beta_i^{(K)}=\sum_{j\in \text{Top-}K} w_{ij}c_{ij}.
\end{equation}
对于当前可用的Top-5样本，$\sum_{j\in \text{Top-}5} w_{ij}=0.360897$，
部分贡献 $\alpha_i^{(5)}=0.1804485$ 和 $\beta_i^{(5)}=0.2153186$，
表明高权重任务（通常是测试/维护/需求/协作）将主导聚合冲击。


\subsubsection{最小校准：带有限人工审查的类别默认值}

为避免重新收集数据，我们仅通过一小组任务类别校准 $s/c$，同时保持任务列表和权重 $w_{ij}$ 不变。
令 $\kappa(j)$ 将每个任务映射到一个粗类别（如需求/利益相关者、编码/实现、测试/验证/安全、监控/运维），并带有默认 $(s,c)$ 对。
然后我们仅手动审查少量高权重任务并记录简要证据说明，在保持可重现性和透明性的同时提高 $(\alpha_i,\beta_i)$ 的可识别性。
