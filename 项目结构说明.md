# 项目结构说明

## 📁 目录结构

```
ICM-F/
├── main.tex                    # 主 LaTeX 文档（基于 mcmthesis v6.3.3）
├── figures/                     # 存放图表文件（PDF 格式推荐）
├── code/                       # 存放代码文件（Python/MATLAB 等）
|—— data/                       # 存放原始数据集、中间处理结果（.csv,.xlsx,.json）
├── sections/                   # 存放分章节 LaTeX 文件（可选，用于模块化写作）
├── build/                      # LaTeX 编译输出目录（自动生成）
├── .cursor/                    # Cursor 规则体系目录
│   └── rules/                  # MCM/ICM 写作规则
│       ├── core-identity.mdc
│       ├── core-mcm-voice.mdc
│       ├── latex/
│       ├── workflows/
│       └── python/
├── .venv/                      # Python 虚拟环境（uv 创建）
├── .gitignore                  # Git 忽略文件配置
├── .cursorignore               # Cursor 忽略文件配置
├── pyproject.toml              # Python 项目配置文件
├── README.md                   # 项目说明文档
└── Cursor规则文件说明.md       # 规则体系详细说明
```

### 目录说明

#### 核心文件
- **`main.tex`**: 主文档文件，基于 mcmthesis v6.3.3 模板构建，包含完整的论文结构和 2026 年 AI 披露协议支持

#### 资源目录
- **`images/`**: 存放论文中使用的所有图表文件
  - 推荐使用 PDF 格式以保持矢量质量
  - 符合 `visualization.mdc` 规则中的图表标准

- **`code/`**: 存放模型实现代码
  - Python 脚本（`.py`）
  - MATLAB 脚本（`.m`）
  - 用于与论文描述进行一致性校验

- **`sections/`**: 可选的分章节文件目录
  - 可以将论文各章节拆分为独立文件，使用 `\input{}` 引入
  - 便于团队协作和版本管理

- **`build/`**: LaTeX 编译输出目录
  - 由 `latexmk` 自动生成
  - 包含 `.aux`, `.log`, `.pdf` 等编译产物
  - 已在 `.gitignore` 中排除

#### 配置目录
- **`.cursor/rules/`**: Cursor AI 规则体系
  - 包含 9 个专业的 MCM/ICM 写作规则文件
  - 自动应用于 LaTeX 和 Python 文件

- **`.venv/`**: Python 虚拟环境
  - 使用 `uv` 创建
  - Python 版本：3.14.0

## 🚀 下一步操作

### 1. 修改主文档配置

在 `main.tex` 文件中，需要修改以下配置：

```latex
\mcmsetup{
  tcn = {0000000},            % 修改为您的控制编号（例如：2312345）
  problem = {A},              % 修改为您选择的题号 (A, B, C, D, E, F)
  ...
}
```

**重要提示**：
- `tcn`：比赛开始后，COMAP 会分配控制编号，必须准确填写
- `problem`：根据您选择的题目修改（A-F 对应不同题目）

### 2. 编译 LaTeX 文档

使用 `latexmk` 进行编译（符合 `build-toolchain.mdc` 规则）：

```bash
# 编译文档
latexmk -pdf -interaction=nonstopmode -synctex=1 -outdir=build main.tex

# 清理编译产物
latexmk -c
```

**编译输出**：
- PDF 文件将生成在 `build/main.pdf`
- 如果编译出错，检查 `build/main.log` 文件

### 3. 开始写作

#### 3.1 使用 Cursor AI 辅助

规则体系会自动应用，Agent 会：
- ✅ 实时检查人称、语态、时态使用
- ✅ 提示为假设添加理由
- ✅ 验证摘要是否包含所有必需要素
- ✅ 检查数学公式排版
- ✅ 验证代码与论文描述的一致性

#### 3.2 添加图表

1. 使用 Python 生成图表（遵循 `visualization.mdc` 规则）：
   ```python
   import matplotlib.pyplot as plt
   # 生成符合学术标准的图表
   plt.savefig('images/figure1.pdf')
   ```

2. 在 `main.tex` 中插入：
   ```latex
   \begin{figure}[htbp]
   \centering
   \includegraphics[width=0.8\textwidth]{images/figure1.pdf}
   \caption{图表说明}
   \label{fig:figure1}
   \end{figure}
   ```

#### 3.3 添加代码

1. 将代码文件放在 `code/` 目录
2. 在附录中使用 `lstlisting` 环境插入：
   ```latex
   \begin{lstlisting}[language=Python]
   % 代码内容
   \end{lstlisting}
   ```

#### 3.4 模块化写作（可选）

如果论文较长，可以将章节拆分为独立文件：

```latex
% 在 main.tex 中
\input{sections/introduction.tex}
\input{sections/model.tex}
\input{sections/results.tex}
```

### 4. 更新 AI 工具使用报告

在论文末尾的 "Report on Use of AI Tools" 部分，如实记录使用的 AI 工具：

```latex
\section*{Report on Use of AI Tools}
\begin{enumerate}
    \item \textbf{工具名称}: 具体用途说明
    \item \textbf{工具名称}: 具体用途说明
\end{enumerate}
```

**注意**：此部分不计入 25 页限制，但必须如实填写。

### 5. 检查清单

在提交前，使用以下清单检查：

- [ ] 控制编号（tcn）已正确填写
- [ ] 题号（problem）已正确选择
- [ ] 摘要包含：问题重述、方法、结果、敏感性分析
- [ ] 所有假设都包含理由
- [ ] 使用第一人称复数（"We"）
- [ ] 数学公式格式正确
- [ ] 图表有标签和单位
- [ ] 代码与论文描述一致
- [ ] AI 工具使用报告已填写
- [ ] 总页数不超过 25 页（不含 AI 报告）
- [ ] 参考文献格式正确

### 6. 团队协作建议

- 使用 Git 进行版本控制
- 定期提交更改
- 使用 `sections/` 目录拆分章节，便于并行写作
- 在提交前进行最终合并和格式检查

---

**祝您在 MCM/ICM 竞赛中取得优异成绩！** 🏆
